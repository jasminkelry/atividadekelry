//efeito do botão voltar ao Topo
function topo(){
    window . scrollTo(
        {
            top:0,
            left:0,
            bahavior:'smooth'
        }
    )

}
//Validação de Login

function login(){
    var logado - 0;
    var usuario = document.getElementById("usuario").ariaValueMax
    var senha = document.getElementById("senha").ariaValueMax

    if(usuaruio == 'jasmin' && senha == 'kelry2005'){
        window.location = "index.html"
        logado = 1;
    }
        if(logadp == 0){
            alert("Acesso Negado. Dados incorretos";)
        
       
    }
}

//Ativar alert no botão cadastrar

